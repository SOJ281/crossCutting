# crossCutting

taskApp is the folder containing the relevant application for review.
models.py shows the SQL data structure and validation
views.py has the code for each POST/GET request
tests.py shows unit tests



API endpoints

http://127.0.0.1:8000/                  -   view tasks in database
http://127.0.0.1:8000/add/              -   GET HTML form for adding new tasks
http://127.0.0.1:8000/add/addrecord/    -   POST form that adds tasks to database